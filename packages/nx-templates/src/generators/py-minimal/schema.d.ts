/**
 * @description
 * Interface for the py-minimal generator options.
 * 
 * Checkout for [README.md](./README.md) for each argument usage.
 */
export interface PyMinimalGeneratorSchema {
  name: string;
  parent?: string;
  prefix?: string;
}
